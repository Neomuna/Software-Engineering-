extends layout
block content
    - console.log(data)
    .main-content 
    h1 All User 
    table(border=1)
        for item in data 
            tr  
                td #{item.id}
                td 
                    a(href='/User-single/' + item.id) #{item.name}