extends layout
block content
    - console.log(data)
    .main-content 
    h1 language  
    table(border=1)
        for item in data 
            tr  
                td #{item.id}
                td 
                    a(href='/Language/' + item.id) #{item.name}